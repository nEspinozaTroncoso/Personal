# Backlog — Pan App

Backlog vivo del proyecto. Lo mantiene el agente **`gestor-backlog`** (model: opus).
Prioridades: `P1` alto · `P2` medio · `P3` bajo. Estados: `pendiente` · `en progreso` · `hecho` · `descartado`.

> Convención: los IDs son estables y no se reutilizan. Los items `hecho`/`descartado` se conservan
> para trazabilidad. Cuando un item necesita diseño, se pasa a `plan-panadero`; su plan queda en
> `docs/plans/<slug>.md` y se enlaza en la columna Notas.

## Activo

| ID | Título | Prioridad | Estado | Notas |
|----|--------|-----------|--------|-------|
| _(sin items activos)_ | | | | Backlog al día: no queda ningún item `pendiente` ni `en progreso`. |

## Historial (hecho / descartado)

| ID | Título | Estado | Notas |
|----|--------|--------|-------|
| B-00 | Implementación general v1 (scaffold Vite + refactor modular + deploy Netlify) | hecho | Plan: `docs/plans/b-00-implementacion-general-v1.md`. `npm install` + `npm run build` ok; paridad de cálculo verificada. `BreadApp.jsx` conservado como referencia. |
| B-01 | Tests unitarios de `baker.js` con Vitest | hecho | Plan: `docs/plans/b-01-tests-baker.md`. 10 tests en verde (`npm test`), build sin regresión. Observación anotada: `formatAmount(9.96) === "10.0"` fijado como comportamiento real. |
| B-02 | PWA instalable + offline (manifest + service worker) | hecho | Plan: `docs/plans/b-02-pwa.md`. `vite-plugin-pwa` ^0.20.5, íconos PNG generados, precache de 17 entradas. Tests 10/10, build ok, `sw.js`+`manifest.webmanifest` en `dist/`. `src/` intacto. |
| B-04 | Modo oscuro con toggle | hecho | Plan: `docs/plans/b-04-modo-oscuro.md`. CSS custom properties (`:root` / `[data-theme="dark"]`); hook `useTheme` (persiste en `panapp-theme`, respeta `prefers-color-scheme`); toggle accesible en Header; anti-FOUC + `theme-color` runtime. Tests 10/10, build ok, PWA intacta. |
| B-03 | Persistir receta/peso favorito en `localStorage` | hecho | Plan: `docs/plans/b-03-persistencia-localstorage.md`. Persistencia inline en `useRecipeScaling` (claves `panapp-recipe`/`panapp-weight`), lazy init validado + `try/catch`. API del hook sin cambios; `App.jsx`/componentes intactos. Tests 10/10, build ok. Observación: si aparece un 3.º valor persistido, extraer helper `usePersistentState` y unificar con `useTheme`. |
| B-05 | Buscador/filtro de recetas | hecho | Plan: `docs/plans/b-05-buscador-recetas.md`. Estado local en `RecipeSelector`; filtra `name`+`subtitle` con `normalize("NFD")` (insensible a acentos), `type="search"` accesible, "sin resultados" con `role="status"`. No cambia `activeId`. Solo toca `RecipeSelector.jsx` + `global.css`. Tests 10/10, build ok. |
| B-06 | Integración con API de recetas de pan (TheMealDB) | hecho | Plan: `docs/plans/b-06-integracion-api-recetas.md`. Implementado en `src/lib/mealdb.js`, `src/hooks/useExternalRecipes.js`, `src/components/ExternalRecipes.jsx`. **Corrección en verificación manual**: el plan asumía `filter.php?c=Bread` (categoría inexistente → `{meals:null}`); se cambió a `search.php?s=bread` (confirmado con curl). Bug no cubierto por los tests actuales (solo normalizadores puros) → seguimiento en **B-10**. |
| B-07 | Internacionalización (es/en) | hecho | Plan: `docs/plans/b-07-internacionalizacion.md`. Sistema i18n propio sin dependencias nuevas: `src/i18n/messages.js` (diccionario plano `{es, en}`, ~26 claves), `src/i18n/translate.js` (`translate()` pura con interpolación `{param}` y fallback a `es`), `src/i18n/translate.test.js` (incluye test de paridad de claves es/en) y hook `src/hooks/useI18n.js` (calcado de `useTheme`: persiste en `panapp-locale`, respaldo `navigator.language`, fija `document.documentElement.lang` y `document.title`). Distribución de `t` por prop-drilling desde `App.jsx` a todos los componentes (sin Context, mismo criterio que B-04); toggle de idioma en Header junto al de tema. **Alcance: solo el chrome/UI**; el contenido de recetas locales (`src/data/recipes.js`) y el de TheMealDB (B-06) quedan fuera de alcance → traducir `recipes.js` anotado como candidato futuro (requeriría rediseñar el modelo de datos). Tests 33/33 en verde (incl. paridad de claves), build sin regresiones, verificación E2E manual con Playwright (idioma inicial, toggle, persistencia tras reload, combinación con modo oscuro, sin errores de consola). |
| B-11 | Buscador libre de recetas externas (TheMealDB) | hecho | Plan: `docs/plans/b-11-buscador-libre-mealdb.md`. **Parte 1/4 del grupo "Evolución recetas externas + favoritos"** (reactivado por el usuario; se aborda en orden B-11→B-12→B-13→B-14). `fetchBreadList(term = BREAD_SEARCH_TERM, signal)` en `src/lib/mealdb.js` parametrizado con `encodeURIComponent`. `useExternalRecipes.js` gana estado `query`/`setQuery` (inicial `"bread"`), debounce de 400ms con `setTimeout` a mano (sin librería nueva) + `AbortController` reutilizado, y mínimo de 2 caracteres antes de disparar red (estado `"prompt"` por debajo del mínimo). Input de búsqueda accesible en `ExternalRecipes.jsx` (mismo patrón `type="search"` que `RecipeSelector` de B-05), prellenado con "bread". Claves i18n nuevas en `messages.js` (`external.searchPlaceholder`, `external.searchAria`, `external.noResults` con interpolación del término, `external.typePrompt`); se retiró `external.empty` (paridad es/en mantenida). Corregido el test de B-10 (`mealdb.fetch.test.js`) que asumía `signal` como primer argumento posicional; añadida cobertura del término libre codificado. `npm test` 47/47 en verde, `npm run build` sin regresiones. Verificación E2E manual con Playwright: estado inicial correcto, debounce colapsa tecleo rápido a 1 petición, término de 1 char no dispara red (muestra prompt), término sin resultados muestra mensaje con el término interpolado, y — caso clave — cambiar de término antes de que venza el debounce anterior no genera resultados obsoletos (solo dispara la petición del término final). Sin errores de consola. |
| B-12 | Importar receta externa al calculador principal (métrico g/ml) | hecho | Plan: `docs/plans/b-12-importar-receta-externa-metrico.md`. **Parte 2/4 del grupo "Evolución recetas externas + favoritos"** (secuencia B-11→B-12→B-13→B-14). El item más pesado y de mayor riesgo del grupo; las **tres decisiones de diseño delegadas a `plan-panadero`** quedaron resueltas así: **(a)** ni preguntar ni adivinar conversiones ambiguas; **(b)** solo se convierten unidades exactas (masa: mg/g/kg/oz/lb → g; volumen: ml/cl/dl/l → ml, factores fijos tabulados), y todo lo ambiguo (`cup`, `tbsp`, `tsp`, conteos como "2 eggs", "to taste") se muestra tal cual con advertencia visual — **sin tabla de densidades**; **(c)** la receta importada se guarda como **pesos fijos, sin `pct`** — no pasa por `scaleRecipe` ni usa el slider de peso, dejando `src/lib/baker.js` y `src/data/recipes.js` intactos (respeta la decisión de B-06 que descartó convertir externas a `pct` por frágil). Implementación: módulo puro `src/lib/importRecipe.js` (+ `importRecipe.test.js`, 25 tests) con la tabla de conversión exacta y parser de measures (fracciones ascii/unicode, mixtas, singular/plural de unidades); hook `src/hooks/useImportedRecipe.js` (estado de sesión, no persiste); componente `src/components/ImportedRecipe.jsx` (reutiliza `StepList`/`formatAmount`, banner `role="status"`, badge ⚠ por ingrediente no convertido); botón "Cargar en el calculador" en el detalle de `src/components/ExternalRecipes.jsx`; render condicional en `src/App.jsx` (oculta `WeightControl`/`ShareButton` con receta importada activa, se limpia al elegir una local). 6 claves i18n nuevas en `src/i18n/messages.js` con paridad es/en. `npm test` 72/72 en verde, `npm run build` sin regresiones, y verificación E2E manual con Playwright (el implementador no había probado el flujo en navegador) con "Rye bread" (caso mixto: gramos exactos + tsp/tablespoon ambiguos): conversión correcta de exactas, badges en las ambiguas, `WeightControl`/`ShareButton` ocultos durante la importación, limpieza al volver a una receta local, y reimportación tras limpiar sin estado raro. Sin errores de consola. |
| B-13 | Favoritos persistentes (recetas locales + externas) | hecho | Plan: `docs/plans/b-13-favoritos-persistentes.md`. **Parte 3/4 del grupo "Evolución recetas externas + favoritos"** (secuencia B-11→B-12→B-13→B-14). Las **cuatro decisiones de diseño delegadas a `plan-panadero`** quedaron resueltas así: **(a) esquema de almacenamiento** — una sola clave `panapp-favorites` en `localStorage` con un array de registros `{kind, id}` (locales) / `{kind, id, name, thumb}` (externas); `kind` actúa como namespace y evita la colisión slug-vs-`idMeal`; **(b) identidad externa** — al marcar se guarda una copia mínima (id+nombre+thumb), sin depender de volver a pedir a la API; **(c) `usePersistentState` descartado** — tras evaluar el código real de `useTheme`/`useRecipeScaling` (demasiado heterogéneos para una abstracción común sin fugas), en su lugar se extrajo solo la lógica pura de lista a `src/lib/favorites.js`, testeable y sin React (revierte la observación de B-03 sobre unificar persistencia); **(d) UI** — toggle ★/☆ accesible (`aria-pressed`/`aria-label` dinámico) en pestañas locales (`RecipeSelector.jsx`), tarjetas y detalle externo (`ExternalRecipes.jsx`) y receta importada (`ImportedRecipe.jsx`), implementado con wrapper `position:relative` + estrella hermana para evitar anidar `<button>` dentro de elementos que ya son botones. Implementación: `src/lib/favorites.js` (+ `favorites.test.js`, 16 tests: namespace `kind:id`, alta/baja sin mutar, saneado de datos malformados), `src/hooks/useFavorites.js` (persistencia inline, sin helper genérico), y 6 archivos modificados (`App.jsx`, `RecipeSelector.jsx`, `ExternalRecipes.jsx`, `ImportedRecipe.jsx`, `messages.js` con 2 claves nuevas es/en, `global.css` con `.fav-toggle`). **Fuera de alcance explícito**: el reordenamiento del selector por favoritas (es **B-14**; no se tocó el orden de `filtered.map` en `RecipeSelector.jsx`). Nota de proceso: la implementación se cortó a mitad por límite de sesión del agente (en `RecipeSelector.jsx`) y se retomó desde el mismo agente sin duplicar trabajo, verificado con `git status`/`git diff --stat` antes de continuar. `npm test` 88/88 en verde, `npm run build` sin regresiones, y verificación E2E manual con Playwright: el toggle local marca/desmarca sin cambiar la receta activa y persiste tras reload, el toggle en tarjeta externa NO abre el detalle al clicar (aislamiento de eventos correcto), `localStorage` guarda exactamente la copia mínima esperada (`{kind:"mealdb", id, name, thumb}`), y funciona también en la receta importada. Sin errores de consola. |
| B-14 | Ordenar recetas por favoritas al abrir la app | hecho | Plan: `docs/plans/b-14-ordenar-favoritas.md`. **Parte 4/4 (última) del grupo "Evolución recetas externas + favoritos"** (secuencia B-11→B-12→B-13→B-14) — **con este item el grupo queda 100% cerrado**. Las **cuatro decisiones de diseño delegadas a `plan-panadero`** quedaron resueltas así: **(a) orden secundario** — se mantiene el orden original de `RECIPES` dentro de cada grupo (favoritas / no favoritas) vía sort estable; **(b) interacción con el buscador de B-05** — el orden por favoritas se aplica siempre, encima del filtro, como criterio único de orden; **(c) solo al montar, no en vivo** — el orden se congela en un snapshot con inicializador perezoso de `useState` al montar `RecipeSelector`; las estrellas ★/☆ se siguen actualizando en vivo (vía `isFavorite`) pero las posiciones no saltan hasta la próxima recarga, evitando el riesgo de click accidental si el usuario desmarca la receta activa; **(d) alcance solo local** — las favoritas externas de TheMealDB quedan fuera (`RecipeSelector.jsx` solo renderiza `RECIPES` locales). Implementación: **un único archivo tocado**, `src/components/RecipeSelector.jsx` — nuevo `orderedRecipes` vía `useState(() => ...)` con inicializador perezoso, reutilizando `isFavorite("local", r.id)` ya existente sin duplicar lógica; el `filtered.map` de B-05 pasa a operar sobre `orderedRecipes` en vez de `recipes`. Sin dependencias nuevas, sin cambios en `App.jsx`, i18n ni CSS. `npm test` 88/88 en verde, `npm run build` sin regresiones, y verificación E2E manual con Playwright: orden inicial correcto, marcar favoritas en vivo NO reordena hasta el próximo reload, tras reload el orden coincide con lo esperado (favoritas primero, orden relativo original preservado dentro de cada grupo), marcar la receta activa como favorita en vivo no le hace "saltar" de posición, y el buscador de B-05 sigue funcionando sobre la lista reordenada. Sin errores de consola. |
| B-10 | Reforzar tests de la integración TheMealDB (fetch + URL + errores) | hecho | Plan: `docs/plans/b-10-tests-integracion-mealdb.md`. Nuevo `src/lib/mealdb.fetch.test.js` con 10 tests que mockean `global.fetch` (`vi.stubGlobal`/`vi.fn()`, sin dependencias nuevas) cubriendo `fetchBreadList`/`fetchMealDetail`: URL exacta (incl. `encodeURIComponent` en `fetchMealDetail`), propagación de `signal`, caso feliz (fetch→normalizador), HTTP no-OK y rechazo de red. Verificado por "romper y revertir" que el test de URL falla con el endpoint erróneo `filter.php?c=Bread` de B-06. `useExternalRecipes` queda fuera de alcance (no justifica añadir `@testing-library/react`). Tests 28/28 en verde (`npm test`), build sin regresiones. |
| B-09 | Normalizar redondeo de `formatAmount` cerca de 10 | hecho | Plan: `docs/plans/b-09-redondeo-formatamount.md`. `formatAmount` en `src/lib/baker.js` ahora calcula `rounded1 = Math.round(n * 10) / 10` y decide la rama decimal/entero sobre ese valor **ya redondeado** (umbral efectivo 10 → 9.95). Se eligió "redondear primero, decidir después" sobre comparar contra el literal `9.95` porque ese literal no es exactamente representable en flotante (abriría una franja de discrepancia microscópica en el borde); el enfoque elegido es auto-consistente por construcción. `scaleRecipe` sin tocar. Test actualizado: `formatAmount(9.96)` pasa de `"10.0"` a `"10"`; añadidos casos límite `9.949999→"9.9"`, `9.95→"10"`, `9.999→"10"`, `3.456→"3.5"`, `234.6→"235"` (más los previos `10→"10"`, `10.4→"10"`, `10.6→"11"`). Único consumidor en build, `src/components/IngredientList.jsx`, sin cambios (solo renderiza el string). `npm test` 33/33, `npm run build` sin regresiones. |
| B-08 | Compartir receta escalada por URL (query params) | hecho | Plan: `docs/plans/b-08-compartir-receta-url.md`. Nuevo módulo puro `src/lib/shareUrl.js` (+ `shareUrl.test.js`, 13 tests): `parseShareParams(search, {recipeIds, minWeight, maxWeight})` y `buildShareUrl(baseUrl, recipeId, weight)`, sin importar `RECIPES` directo (restricciones inyectadas) y sin dependencias nuevas (`URLSearchParams` nativo, nada de React Router). Precedencia en los inicializadores perezosos de `useRecipeScaling.js`: **URL válida → `localStorage` (B-03) → default**; tras sembrar el estado, la URL se limpia con `history.replaceState` (link de un solo uso — un reload no reimpone el link). Params inválidos (receta inexistente, peso fuera de rango) degradan con gracia hacia `localStorage`/default sin crashear. Nuevo `src/components/ShareButton.jsx` con copiar al portapapeles (`navigator.clipboard.writeText`) y feedback accesible (`role="status"`/`role="alert"` + fallback de URL seleccionable), montado en `App.jsx` tras `WeightControl`. 4 claves nuevas en `src/i18n/messages.js` (es/en, paridad respetada). Confirmado que el redirect SPA de `netlify.toml` no necesita cambios. `npm test` 46/46 en verde, `npm run build` sin regresiones, verificación E2E manual con Playwright (clipboard real incluido): siembra receta+peso y limpia la URL, precedencia URL→localStorage→default en ambas direcciones, degradación con params inválidos, copiado con contenido correcto, sin crashes en doble-click ni errores de consola. |

---

### Siguiente item recomendado
**No hay item que recomendar: el backlog no tiene nada pendiente.** Con **B-14 cerrado**, el grupo
**"Evolución recetas externas + favoritos"** (**B-11 → B-12 → B-13 → B-14**) queda **100% completo**,
y con él **todo el backlog activo**: no quedan items `pendiente` ni `en progreso`. Los últimos items
sueltos fuera del grupo (B-07 i18n, B-08 compartir por URL, B-09 redondeo, B-10 tests TheMealDB) ya
estaban `hecho` antes de esta tanda.

**Sin siguiente paso para `plan-panadero`.** No hay nada que planificar hasta que el usuario proponga
una idea nueva o reactive algún item futuro. Cuando surja una idea, se agrega aquí como nuevo ID
correlativo (el próximo libre sería **B-15**) y, si necesita diseño, se pasa a `plan-panadero`.

> **Nota:** el grupo **B-11/B-12/B-13/B-14** ("Evolución recetas externas + favoritos", P2/P3) se
> abordó en el orden de sus índices **B-11 → B-12 → B-13 → B-14** y está **cerrado por completo**.
> Candidatos futuros anotados en el histórico si el usuario quiere retomarlos: traducir el contenido
> de recetas locales (`src/data/recipes.js`, ver B-07) y traducir/normalizar el contenido de
> TheMealDB (B-06/B-07).
